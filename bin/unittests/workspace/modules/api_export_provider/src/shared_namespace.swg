#global public
#global namespace Shared

// Deliberately shares its name with the namespace the consumer declares, so that the consumer can
// check which one an unqualified lookup picks. See 'consumer_exe/src/shared_namespace.swg'.
const Marker = 1

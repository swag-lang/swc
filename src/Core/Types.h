#pragma once

using Ref                 = uint32_t;
constexpr Ref INVALID_REF = 0;

using AstNodeRef    = Ref;
using AstPayloadRef = Ref;
using FileRef       = Ref;
using TokenRef      = Ref;
using JobClientId   = Ref;

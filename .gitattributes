# Normalize all text files to LF in the repo
* text=auto eol=crlf

# Windows batch files require CRLF
*.bat text eol=crlf

# Test and application fixtures are byte-exact: a test compares their sizes and hashes
bin/**/datas/** -text

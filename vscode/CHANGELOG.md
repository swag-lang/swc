# Changelog

## 0.0.164

- Color `late`, `tls` and `global` as declaration keywords, in a destructuring binding too.
- Drop the `#null` modifier and the `Swag.isSet` intrinsic, and read `?` as an operator.

## 0.0.163

- Drop `undefined` from the literal keywords.

## 0.0.162

- Add the `Swag.atomget` intrinsic.

## 0.0.161

- Drop the `Swag.index` value.

## 0.0.160

- Base the workbench on VSCode's `Dark (Visual Studio)` palette while keeping Swag's syntax colors.

## 0.0.159

- Use VSCode's native dark workbench palette while keeping Swag's syntax colors.

## 0.0.158

- Give the built-in `Swag` namespace its own syntax color while preserving each member's semantic color.

## 0.0.1

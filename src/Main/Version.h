#pragma once

// Compiler version
inline constexpr auto SWC_VERSION   = 0;
inline constexpr auto SWC_REVISION  = 0;
inline constexpr auto SWC_BUILD_NUM = 0;

// Swag version
inline constexpr auto SWAG_VERSION = SWC_VERSION;

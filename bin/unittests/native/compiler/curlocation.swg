// Migrated from the former native/legit suite (_compiler3279.swg).
#global fileprivate
using Swag

func legitCurLocationEntry()
{
    #run
    {
        #assert(#curlocation.funcName == "legitCurLocationEntry")
    }
}

# Parser tests exercise deliberate whitespace and layout variants: a rewrite
# would erase what they are testing. `preserve` is the style that normalizes
# nothing, so this suite stays exactly as written whatever the canonical style
# gains next.
style = preserve

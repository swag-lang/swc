#global fileprivate
/**
# Where Constraints
`where` constraints on generic declarations are currently documented here, but the
examples below are commented until the compiler behavior matches the reference again.
*/

// @TODO: Re-enable the `where` constraint reference examples in this file when generic
// numeric literals, constrained overload resolution, generic structs, and constexpr
// checks all compile and run as documented again.

#global private
/**
Scopes determine where a name is visible and when cleanup runs. Namespaces
organize module-level symbols; 'using' publishes a scope's names and 'with'
selects a value; 'defer' schedules work at lexical-scope exit. The leading dot
names a member of whichever subject is nearest, under one law stated at the end
of this chapter.
*/
